# Libro.AgentWCAG 跨代理 WCAG Skill Repository — 待辦事項

> 掃描時間：2026-03-28（增量更新 #12 + 決策標註）
> 目標：完善跨代理的 WCAG 網頁無障礙 skill repository，讓 Codex、Claude、Gemini、Copilot 以一致方式執行無障礙稽核、提出修正建議，並在明確授權下套用安全範圍內的自動修正。
> 執行計畫：[roadmap-execution-plan.md](roadmap-execution-plan.md)

---

## A. 跨代理一致性（Vendor-Neutral Contract）

- [x] **A1** Copilot 缺少 workspace skill 探索目錄：`.copilot/skills/libro-wcag/SKILL.md` 不存在，Claude（`.claude/`）和 Gemini（`.gemini/`）皆已有對應複本。
- [x] **A2** Codex 缺少 workspace skill 探索目錄：`.codex/skills/libro-wcag/SKILL.md` 不存在，僅有空殼 `environment.toml`。
- [x] **A3** 各 adapter 的 `prompt-template.md` 缺少進階 CLI 選項指引（如 `--policy-bundle`、`--fail-on`、`--baseline`、`--debt-trend-window`），跨代理使用進階功能時需手動查閱 CLI 文件。
  > 已建立 `references/cli-reference.md` 完整 CLI 選項參考文件，並在 4 個 adapter prompt-template 加上進階選項交叉引用。**Batch 2**
- [x] **A4** `skills/libro-wcag/agents/` 僅有 `openai.yaml`，缺少 Claude、Gemini、Copilot 對應的 agent manifest 定義檔。
- [ ] **A5** MCP server 的 `libro_wcag_audit` 工具僅暴露基本參數（target、mode、version），未暴露進階功能（`--policy-bundle`、`--fail-on`、`--baseline`、`--summary-only`、`--report-format sarif`），限制了 MCP 用戶端的能力。
  > 📋 **決策：延後。** MCP 保持輕量入口，等有實際使用回饋再擴充。**Batch 6**
- [ ] **A6** MCP server 缺少 `apply-fixes` 模式的 MCP tool 支援——`libro_wcag_audit` 預設 `audit-only`，且未將修正結果（diff artifacts）回傳給呼叫者。
  > 📋 **決策：延後。** MCP 限制為唯讀稽核+建議，不做 apply-fixes。**Batch 6**

## B. Schema 與合約強化

- [x] **B1** JSON Schema (`wcag-report-1.0.0.schema.json`) 對多數欄位使用 `additionalProperties: true`，缺少對 `findings[].fixability`、`findings[].verification_status`、`findings[].manual_review_required`、`fixes[].fixability` 等 core-spec 定義欄位的顯式驗證。
- [x] **B2** Schema 未定義 `findings[].confidence`、`findings[].sc`、`fixes[].remediation_priority`、`fixes[].framework_hints` 等文件中規定的欄位，使得外部消費者難以做嚴格驗證。
- [x] **B3** Schema 無版本遷移策略——當 schema 從 `1.0.0` 升級時，缺少 `$schema` 版本比較或向後相容驗證合約。
  > 已在 core-spec.md §5 加入 Schema Versioning Policy，記錄 semver 規則、compatibility 合約、升版步驟，以及既有程式碼層級驗證（report_artifacts、baseline_governance、advanced_gates）。**Batch 2**
- [x] **B4** `summary` 物件在 schema 中僅要求 `total_findings`，未驗證 `remediation_lifecycle`、`diff_summary`、`debt_trend` 等已在 core-spec 中記載的欄位。

## C. 測試覆蓋率強化

- [ ] **C1** MCP server (`mcp-server/`) 缺少自動化整合測試——`test_mcp_server.py` 存在但未涵蓋 `apply-fixes` 模式回傳、錯誤處理、及 SARIF 格式輸出。
  > 📋 **決策：延後。** 等 MCP 方向明確後再補。**Batch 6**
- [x] **C2** ~~缺少跨平台（Windows / macOS / Linux）CI 矩陣——`test.yml` 僅在 `ubuntu-latest` 上執行，但 `scanner_runtime.py` 有 Windows 特定的 `npx.cmd` 邏輯未被 CI 驗證。~~ 關閉：GitHub Actions 無法有效驗證 Windows GUI 環境，維持現狀。
  > 📋 **決策：關閉。** GitHub Actions 無法有效驗證 Windows GUI 環境，維持現狀。
- [x] **C3** ~~Skipped tests 未記錄原因~~ — 已確認 6 個 skipped tests 均為 `RealScannerIntegrationTests`，skip 條件為 `LIBRO_RUN_REAL_SCANNERS != '1'`，跳過原因已在 skip message 中說明（`real scanner integration disabled`），屬預期行為。（誤報，已修正）
- [x] **C4** ~~`package.json ↔ pyproject.toml` 版本同步~~ — 已確認 `test_repo_contracts.py` 的 `test_package_json_exposes_global_libro_cli()` 會驗證版本一致。（誤報，已修正）
- [x] **C5** 缺少 MCP tool schema 與 SKILL.md 宣告一致性的合約測試（如：MCP 所暴露的參數預設值需與 SKILL.md 一致）。
  > 已在 `test_repo_contracts.py` 加入 `test_mcp_tool_defaults_match_skill_md_contract()`，用 AST 解析 `server.py` 函式簽名，驗證 `wcag_version`、`conformance_level`、`output_language` 預設值與 SKILL.md 一致，並記錄 MCP audit 故意使用 `audit-only` 的設計決策。**Batch 5**

## D. 發佈與 CI/CD 完善

- [x] **D1** ~~`publish-npm.yml` 缺少 `NODE_AUTH_TOKEN` 的明確文件或 secret 設定說明，新維護者可能無法順利發佈。~~ 關閉：官方目前發布版本已不用 key（OIDC trusted publishing）。
  > 📋 **決策：關閉。** 官方目前發布版本已不用 key（OIDC trusted publishing）。
- [x] **D2** ~~`release.yml` 和 `publish-npm.yml` 分別管理測試與驗證步驟，存在重複邏輯——可提取為 reusable workflow。~~ 關閉：維持現狀，重複度可接受。
  > 📋 **決策：關閉。** 維持現狀，重複度可接受。
- [x] **D3** ~~`plugin.json` / `marketplace.json` 版本同步~~ — 已確認 `scripts/apply-release-version.py` 的 `update_claude_plugin()` 會同步更新 `plugin.json` 和 `marketplace.json`。（誤報，已修正）
- [x] **D4** ~~npm 發佈流程中 `npm publish` 未明確帶 `--provenance` flag——雖然可由 registry 政策觸發，但顯式宣告更安全且可審計。~~ 關閉：目前可正常使用，OIDC 已處理 provenance。
  > 📋 **決策：關閉。** 目前可正常使用，OIDC 已處理 provenance。
- [x] **D5** ~~`dist/` 目錄追蹤問題~~ — 已確認 `dist/` 未被 git 追蹤，僅為本地建置產物。（誤報，已修正）

## E. 文件與開發者體驗

- [x] **E1** 缺少 `CONTRIBUTING.md`——已新增貢獻指南，說明 vendor-neutral contract 守則、adapter 變更邊界、auto-fix 測試要求與 PR 檢查清單。
- [x] **E2** 缺少 `SECURITY.md`——已新增安全性政策，定義私下通報流程、支援範圍與 auto-fix / MCP / 發佈鏈的安全敏感區域。
- [x] **E3** ~~`AGENTS.md` 未被 git 追蹤~~ — 已確認 `AGENTS.md` 在 `dc16c93` 中加入版控。
- [x] **E4** `docs/README.md` 僅為索引頁——已補上各子目錄用途與代表性文件說明。
- [x] **E5** adapter 的 `usage-example.md` 現已補上 Claude / Copilot / Gemini 的 MCP 配置範例交叉引用。
- [x] **E6** `README.md` 和 `README.en.md` 中 Claude Marketplace 安裝指令 (`/plugin marketplace add`) 為自定義語法，需確認是否為已實作的 Claude Code 功能，或僅為預期功能的佔位。
  > 已在 `README.md` 與 `README.en.md` 加上「Coming soon - pending marketplace availability」註記，保留指令作為預告用途。**Batch 1**

## F. 自動修正引擎擴充

- [x] **F1** `auto_fix.py` 目前支援的規則集中於 HTML 靜態重寫——缺少 color contrast（`color-contrast`）的 CSS 修正支援（即使為 suggest-only 降級，也應明確在修正策略庫中標記為不可自動修正並說明原因）。
  > 已在 `remediation_library.py` 為所有 `auto_fix_supported: False` 的規則加上 `auto_fix_reason` 欄位，說明無法自動修正的原因。color-contrast 標記為 "Color choices require design intent"。**Batch 2**
- [x] **F2** `auto_fix.py` 的 regex-based 重寫策略未處理含 `<template>` 或 `<script>` 標籤的複雜 Vue SFC 結構——`rewrite_helpers.py` 有框架支援，但整合路徑未完全測試。
  > 已新增 `vue-sfc-complex.vue` fixture 與 `test_vue_sfc_complex_fixes_template_without_corrupting_script()` 整合測試，驗證 image-alt/button-name/link-name 三項修正正確套用於 `<template>` 區塊且不破壞 `<script setup>` 中的 HTML-like 字串。**Batch 5**
- [x] **F3** ~~`apply-fixes` 模式的 diff artifacts 缺少機器可讀的 per-hunk metadata（affected rule、affected element selector），使得 CI PR annotation 只能做到 file-level 而非 line-level。~~ 關閉（列為下一版）：合約變更影響大，暫不處理。
  > 📋 **決策：關閉（列為下一版）。** 合約變更影響大，暫不處理。

## G. Policy 與 Baseline 治理

- [x] **G1** ~~Policy bundle 驗證不足~~ — `validate_skill.py` 已全面驗證。（已由 `e4019a1` 補強）
- [x] **G2** ~~Baseline governance 文件提到 debt waiver 但缺少 waiver 模板範例——新團隊導入時不易理解如何正確設定 `owner`、`approved_at`、`expires_at`、`reason`。~~ （誤報：`docs/release/baseline-governance.md` 已包含完整 waiver JSON 範例，含 `signature`、`owner`、`approved_at`、`expires_at`、`reason` 五個必要欄位。）
- [x] **G3** `--risk-calibration-source` 和 `--stability-baseline` 的文件範例僅在 release docs 中，缺少 adapter-level 或 MCP-level 的整合指引。
  > 已在四個 adapter 的 `usage-example.md` 補上「Advanced: Baseline & Risk Calibration」區段，含 CLI 範例與 `run_meta` 輸出說明，並交叉引用 `cli-reference.md`。**Batch 自動化**

## H. 安全性與供應鏈

- [x] **H1** `mcp-server/server.py` 中 `audit_page.py` 透過 `subprocess.run` 執行外部命令——target 參數來自 MCP 呼叫者，需確認是否有 shell injection 防護（目前 `shell=False` 是正確的，但 `target` 字串仍可能含有路徑穿越）。
  > 已在 `audit_page.py` 加入 `_validate_target()` 函式，對本地路徑使用 `Path.resolve()` + `relative_to(REPO_ROOT)` 驗證，阻止路徑穿越；URL target 直接放行。**Batch 4**
- [x] **H2** ~~`.pyc` 檔案追蹤問題~~ — 已確認 `.pyc` 未被 git 追蹤，`.gitignore` 規則生效。（誤報，已修正）
- [x] **H3** `mcp-server/requirements.txt` 僅列出 `mcp>=1.2,<2`，未鎖定 hash——生產部署時應提供 `requirements.lock` 或 hash-pinned 版本。
  > 已使用 `pip-compile --generate-hashes` 產出 `mcp-server/requirements.lock`，包含完整相依鏈與 SHA-256 hash。**Batch 1**

## I. 國際化與語言支援

- [x] **I1** `wcag_workflow.py` 的 `MARKDOWN_COLUMNS` 和 `MARKDOWN_LABELS` 僅支援 `en` 和 `zh-TW`——`output_language` 合約接受任意 BCP-47 值，但實際只有兩種語言的 fallback，缺少文件說明此限制。
  > 已在 SKILL.md 和 core-spec.md 限縮 `output_language` 為 `en | zh-TW`，說明其他 BCP-47 值 fallback 為 en，保留 dict 擴充設計。**Batch 2**
- [x] **I2** adapter prompt-template 中的指示文字（如 Claude adapter 的 "Execution requirements"）為英文，但預設 `output_language=zh-TW`——未說明 prompt 語言與 output 語言的分離策略。
  > 已在 core-spec.md §6 和 SKILL.md Adapter Rules 明確記錄分離策略：prompt 指示固定為英文，僅報告輸出（Markdown 欄位、摘要標籤）依 `output_language` 切換。**Batch 2**

## J. 效能與可擴充性

- [x] **J1** ~~Lighthouse headless 問題~~ — 已確認 `scanner_runtime.py:336` 使用 `--headless=new` 啟動 Chrome，CI 環境已處理。（誤報，已修正）
- [x] **J2** `run_accessibility_audit.py` 目前為串行執行 axe → Lighthouse——兩個掃描器相互獨立，可平行執行以縮短審計時間。
  > 已改用 `concurrent.futures.ThreadPoolExecutor` 平行執行 axe 和 Lighthouse（當兩者均需執行時）；單一掃描器或 mock/skip 情境維持原有串行路徑。**Batch 4**
- [x] **J3** ~~去重邏輯 O(n²)~~ — 已確認 `_dedupe_findings()` 使用 dict，時間複雜度為 O(n)。（誤報，已修正）

## K. 例外處理與健壯性

- [x] **K1** `scanner_runtime.py` 中 `_try_run_axe()` 與 `_try_run_lighthouse()` — 已改為回傳含檔案路徑與行列位置的友善錯誤訊息。
- [x] **K2** `wcag_workflow.py` 的 `load_json_file()` — 已改為拋出含檔案路徑與行列位置的 `ValueError`。
- [x] **K3** `scanner_runtime.py` 的 `_run_command()` 現已區分 `PermissionError` 與暫時性 `OSError`。
- [x] **K4** ~~`subprocess.TimeoutExpired` 錯誤訊息過於泛用~~ — 已確認使用 `err.cmd` 與 `err.timeout`。（誤報，已修正）
- [x] **K5** `scanner_runtime.py` 的 `_wait_for_debug_port()` 現已改用剩餘 timeout 計算。

## L. 安裝流程與版本管理

- [x] **L1** `scripts/install-agent.py` 中 `path.parent.mkdir()`、`path.write_text()`、`write_manifest()` 缺少 `PermissionError` / `OSError` 處理——安裝中途失敗會留下不完整狀態且無 rollback。
  > 已在 `install()` 加入 `_backup_existing()` + `_rollback()` 機制：安裝前備份、失敗時自動還原、成功後清除備份。`PermissionError`/`OSError` 均有友善錯誤訊息。**Batch 4**
- [x] **L2** `install-agent.py` 的 `install()` 函式使用 `--force` 取代既有安裝但：未檢查版本相容性、未備份舊版、未警告降級操作、無版本遷移邏輯。
  > 已加入 `_read_installed_version()` 讀取既有 manifest 版本，force 安裝時比較新舊版本並在降級時發出 stderr 警告。**Batch 4**
- [x] **L3** `scripts/validate_skill.py` 現已驗證各 adapter 目錄存在，且必備文件完整。
- [x] **L4** `scripts/validate_skill.py` 現已顯式檢查 `SKILL.md` frontmatter 必要欄位。

## M. SARIF 輸出完整性

- [x] **M1** `run_accessibility_audit.py` 的 `_report_to_sarif()` 缺少 SARIF 2.1.0 規範要求的 `helpUri`/`help` 屬性、`ruleIndex`、`invocation` 物件——生成的 SARIF 不完全符合規範，限制工具互通性。
  > 已在 `_report_to_sarif()` 加入 `helpUri`（W3C Understanding URL）、`help.text`（WCAG SC 描述）、`ruleIndex`（規則陣列索引）、`invocations`（執行狀態與 target 資訊）。**Batch 自動化**
- [x] **M2** SARIF results 缺少 `contextRegion` 和 `snippet` 屬性——IDE 和分析工具無法顯示問題周圍的程式碼上下文。
  > 已在 `_report_to_sarif()` 加入 `contextRegion`（finding 行號 ±1 行的上下文範圍）和 `snippet.text`（CSS selector），IDE 可顯示問題定位。**Batch 自動化**
- [x] **M3** SARIF URI 生成使用 `local_target.as_posix()`（L280），在 Windows 上產出 `C:/path` 而非 RFC 3986 規範的 `file:///C:/path`——部分 SARIF 消費者可能無法識別。
  > 已在 `_report_to_sarif()` 改用 `Path.resolve().as_posix()` + RFC 3986 `file:///` 前綴，Windows 與 Unix 路徑均正確轉換。**Batch 自動化**
- [x] **M4** ~~`report_format` 未驗證~~ — 已確認 `argparse` 使用 `choices=["json", "sarif"]` 在 CLI 層級驗證。（誤報，已修正）

## N. 資源管理與清理

- [x] **N1** ~~file server thread 清理問題~~ — 已確認 `_try_run_lighthouse()` 的 `finally` 區塊會正確清理。（誤報，已修正）
- [x] **N2** `skills/libro-wcag/scripts/` 和 `tests/` 現已補上 `__init__.py`。
- [x] **N3** `skills/libro-wcag/scripts/py.typed` 已補上。

## O. 文件與實作落差

- [x] **O1** `adapters/copilot/e2e-example.md` 聲稱 create 模式在目標不存在時會「產出 manual-review guidance 而非假掃描結果」，但 `scanner_runtime.py` 實際會以「Target file does not exist」錯誤中斷，無法優雅降級。
  > 已在 `run_accessibility_audit.py` 實作 create mode 降級路徑：當 `task_mode=create` 且目標不存在時，跳過掃描器執行，設定 guidance-only 錯誤訊息，讓 normalize_report 產出 manual fallback findings。**Batch 4**
- [x] **O2** 單一 scanner 失敗時，`wcag_workflow.py` 會在 `notes` 和 `summary.scanner_failures` 中記錄並插入 manual fallback finding，但 Markdown 輸出的摘要行未以醒目方式（如警告標記）提示報告基於不完整資料——使用者可能忽略 notes 區段中的 scanner 失敗訊息。
  > 已在 Markdown 摘要加入 `⚠️` 警告行，顯示失敗 scanner 與分類（如 `lighthouse (timeout)`）。**Batch 1**
- [x] **O3** `run_accessibility_audit.py` 現已在 staging report schema artifact 前交叉驗證 schema 檔中的 `report_schema.version.const` 是否與 `REPORT_SCHEMA_VERSION` 一致，避免版本不一致被靜默帶入輸出。

## P. Policy Bundle 驗證深化

- [x] **P1** `validate_skill.py` 的 `_validate_single_policy_bundle()` 未驗證 `include_rules` / `ignore_rules` 中的 rule ID 是否為有效的 axe/lighthouse 規則——可建立含不存在規則的 policy bundle 而無警告。
  > 已在 `validate_skill.py` 加入 `_load_known_rules()` 從 `SCANNER_RULE_TO_SC` 和 `RULE_STRATEGIES` 載入已知規則集，對 policy bundle 中的未知 rule ID 發出 stderr 警告。**Batch 4**

## Q. Reusable Workflow 與消費者體驗

- [x] **Q1** `install-skill.yml` 現已改用 `${{ github.repository }}`，fork 使用者可直接呼叫 reusable workflow。
  > 📋 **決策：** 改為 `${{ github.repository }}`。**Batch 1**
- [x] **Q2** `install-skill.yml` 現已提供 `installation_valid` workflow output，呼叫方可依 `doctor` 驗證結果做條件判斷。
  > 📋 **決策：** 加 output。**Batch 1**
- [x] **Q3** `libro-wcag-real-scanner.yml` 的 SARIF 建置步驟使用 inline Python heredoc 直接匯入 `run_accessibility_audit` 內部函式 (`_report_to_sarif`)——此為私有 API，缺少穩定性合約保障。
  > 已將 inline Python heredoc 替換為 CLI `--report-format sarif` 參數，移除對 `_report_to_sarif` 私有 API 的直接匯入。**Batch 3**

## R. 程式碼品質與可維護性

- [x] **R1** `auto_fix.py` 中多個 `_fix_*` 函式（`_fix_button_name`、`_fix_link_name`、`_fix_role_name`）共享幾乎相同的「找到無 accessible name 的元素 → 注入 aria-label」邏輯——現已提取共用 `_fix_missing_accessible_name(...)` helper，保留既有輸出語意並減少重複。**Batch 3**
- [x] **R2** `rewrite_helpers.py` 已改用 `re.Pattern` 型別註記。
- [x] **R3** `wcag_workflow.py` 中 `AXE_RULE_TO_SC` 和 `LIGHTHOUSE_RULE_TO_SC` 存在大量重複的 rule → SC 對應——現已合併為單一 `SCANNER_RULE_TO_SC` 來源，並導出既有 `AXE_RULE_TO_SC` / `LIGHTHOUSE_RULE_TO_SC` 介面以維持相容。**Batch 3**
- [x] **R4** `scanner_runtime.py` 中 `_find_browser_executable()` 已補上 macOS App bundle 路徑（Chrome / Edge，含 `/Applications` 與使用者 `~/Applications`），macOS 使用者不再依賴額外 symlink。
- [x] **R5** `auto_fix.py` 的 `_guess_accessible_name()` 使用多個 attribute 作為 fallback 猜測名稱（aria-label → title → placeholder → name → id），但在產出的 diff description 中僅顯示最終值，未記錄猜測來源——現已於 diff description 加上 `(guessed from: <attr>)`。**Batch 3**

## S. 合約完整性與邊界行為

- [x] **S1** `SKILL.md` 和 `core-spec.md` 定義 `output_language` 預設為 `zh-TW`，但合約聲稱接受任意 BCP-47 值。`wcag_workflow.py:resolve_contract()` 未驗證 `output_language` 是否為有效的 BCP-47 tag——無效值會被靜默接受並在輸出中原樣使用。
  > 已與 I1 合併處理：文件限縮為 en/zh-TW，`_language_key()` 已有 fallback 邏輯，保留 dict 擴充設計。**Batch 2**
- [x] **S2** ~~`normalize_report.py` CLI 的 `--output-json` / `--output-md` 路徑未檢查父目錄是否存在~~ — 已確認 [`write_report_files()`](C:\Source\Libro.AgentWCAG.clean\skills\libro-wcag\scripts\wcag_workflow.py) 會自動建立父目錄，CLI 可正常輸出到不存在的巢狀目錄。（誤報，已修正）
- [x] **S3** `wcag_workflow.py` 的 `build_citation_url()` 現已依 `wcag_version` 產出對應的 `WCAG20` / `WCAG21` / `WCAG22` Understanding 路徑。
- [ ] **S4** `mcp-server/tools/suggest_fixes.py` 將 `execution_mode` 硬編碼為 `suggest-only`（L30），即使 MCP 呼叫者預期使用其他模式——MCP suggest tool 無法與 `apply-fixes` 整合。
  > 📋 **決策：延後。** 與 A6 連動，MCP 保持唯讀。**Batch 6**

## T. CI 流程健全性

- [x] **T1** ~~`test.yml` 的 `dependency-audit` job 受 `vars.LIBRO_RUN_DEPENDENCY_AUDIT == '1'` 條件控制，但未在 release workflow 中強制執行——可能在含已知漏洞的相依套件下發佈。~~ 關閉：維持可選，專案依賴極少（pyyaml + mcp），漏洞風險低。
  > 📋 **決策：關閉。** 維持可選，專案依賴極少（pyyaml + mcp），漏洞風險低。
- [x] **T2** `publish-npm.yml` 現已在 `npm publish` 前執行 `npm pack --dry-run`，可先驗證打包內容。
  > 📋 **決策：** 加 `npm pack --dry-run` 步驟。**Batch 1**
- [x] **T3** `release.yml` 的 `clean-release-smoke` 僅測試 `codex` agent 的安裝冒煙——未涵蓋 `claude`、`gemini`、`copilot`，無法確保所有 agent zip 套件均可正確解壓安裝。
  > 已改用 `strategy.matrix.agent: [codex, claude, gemini, copilot]`，4 個 agent 並行執行冒煙測試，各自上傳獨立 artifact。**Batch 5**

## U. Adapter 文件與實際輸出對齊

- [x] **U1** 四個 adapter 的 `e2e-example.md` 現已補上 `remediation_lifecycle.verified` 欄位。
- [x] **U2** 文件現已說明 Copilot `"servers"` vs Claude/Gemini `"mcpServers"` 差異。
- [x] **U3** 四個 adapter 的 `e2e-example.md` 現已補上 `run_meta.product` 範例物件。

## V. 報告 Schema 執行時驗證

- [x] **V1** 核心產出路徑在生成 JSON 報告後未執行 runtime schema validation——report schema 檔案存在但從未在產出時被用來驗證結果，消費者無法依賴 schema 保證。
  > 已新增 `test_schema_validation.py`（8 個測試），在測試端使用 `jsonschema` 對 normalize_report 產出的報告做完整 JSON Schema 驗證。同時修正 schema 中 `source_line`/`source_column` 應允許 null 的問題。**Batch 5**
- [x] **V2** 專案未將 `jsonschema` 列為可選依賴——即使未來要加入 runtime validation，也缺少執行環境支援。
  > 已在 `pyproject.toml` 加入 `[project.optional-dependencies] test = ["jsonschema>=4.20"]`。**Batch 5**

## W. 腳本健壯性（非核心流程）

- [x] **W1** `scripts/uninstall-agent.py` 的 `uninstall()` 函式直接呼叫 `shutil.rmtree(destination)` 無 try/except——遇到唯讀檔案、權限不足或檔案鎖定時會以原始例外崩潰，無友善錯誤訊息。
  > 已補 `PermissionError` / `OSError` 包裝與 CLI stderr 訊息，避免 raw traceback 直接暴露給使用者。**Batch 1**
- [x] **W2** `scripts/run-realistic-validation-smoke.py` — 已加入 artifact 載入輔助函式。
- [x] **W3** `scripts/package-release.py` 現已在打包前驗證 release 來源完整性。

## X. 測試 Fixture 與 Snapshot 覆蓋

- [x] **X1** `skills/libro-wcag/scripts/tests/fixtures/` 中有 15 個 HTML fixture 檔案缺少對應的 `.report.json` snapshot baseline——迴歸偵測依賴 inline assertion 而非快照比對，變更影響較難追蹤。
  > 已新增 `generate_snapshots.py` 工具與 15 組 `.report.json` + `.report.md` snapshot baseline，並在 `test_fixtures_and_integration.py` 加入 `test_snapshot_for_remaining_fixtures_matches_expected_output()` 迴歸測試。**Batch 5**
- [x] **X2** ~~現有 snapshot 檔案中的 `remediation_lifecycle` 結構包含 `verified` 欄位，但 adapter e2e-example 範例不包含——snapshot 與文件範例不一致，增加維護混亂。~~ （誤報：四個 adapter 的 e2e-example 已於 U1 補上 `verified` 欄位，所有 21 個 snapshot 與 4 個 e2e-example 的 `remediation_lifecycle` 結構完全一致。）

## Y. Release 打包完整性

- [x] **Y1** `package-release.py` 的 release bundle 現已包含 `CHANGELOG.md`。
- [x] **Y2** `package-release.py` 現已排除 `.tmp`、`.bak` 與 `*~` 暫時檔案。
- [x] **Y3** ~~package-provenance.json 包含策略~~ — `package.json` `files` allowlist 已正確包含。（誤報，已修正）

## Z. WCAG 引用 URL 驗證

- [x] **Z1** `WCAG_UNDERSTANDING_PATHS` 現已補齊至 87 個 SC。
- [x] **Z2** 先前缺漏的常見 SC 已補齊。
- [x] **Z3** 無自動化測試驗證 `WCAG_UNDERSTANDING_PATHS` 中所有 URL slug 是否對應有效的 W3C 頁面——URL 格式正確不代表目標頁面存在。
  > 已在 `test_repo_contracts.py` 加入 `WcagUrlSlugTests`（5 個測試）：slug 格式、SC 編號格式、無重複 slug、三版本 URL 生成、及 HTTP HEAD 可達性（條件式，`LIBRO_RUN_URL_VALIDATION=1` 時啟用）。**Batch 5**

## AA. 文件繁中化與雙語保留

- [x] **AA1** 先盤點正式文件並建立「保留 / 合併 / 移除」清單，避免先翻譯之後又刪掉。執行方式：1) 只檢查 `README.md`、`CONTRIBUTING.md`、`SECURITY.md`、`docs/README.md`、`docs/release/`、`docs/testing/`；2) 為每份文件標記用途（入口、操作、治理、測試、樣本）；3) 標記是否為權威來源或與其他文件重複；4) 將結果回填到本區塊或相鄰決策註記。完成標準：每份正式文件都被歸類，且有明確去留決策。
  > **權威文件名單（保留）**：
  > - `README.md`（入口）、`CONTRIBUTING.md`（治理）、`SECURITY.md`（治理）
  > - `docs/README.md`（索引）
  > - `docs/release/release-playbook.md`（發佈）、`docs/release/apply-fixes-scope.md`（治理）、`docs/release/baseline-governance.md`（治理）、`docs/release/supported-environments.md`（治理）、`docs/release/real-scanner-ci-lane.md`（治理）
  > - `docs/release/ga-definition.md`（治理）、`docs/release/ga-release-workflow.md`（發佈）、`docs/release/advanced-ci-gates.md`（治理）、`docs/release/resilient-run-patterns.md`（操作）、`docs/release/repo-admin-setup.md`（治理）、`docs/release/rollback-playbook.md`（治理）、`docs/release/adoption-smoke-guide.md`（測試）
  > - `docs/release/release-note-template.md`（樣本）、`docs/release/hotfix-release-note-template.md`（樣本）、`docs/release/prompt-invocation-templates.md`（樣本）
  > - `docs/testing/testing-playbook.md`（測試）
  > **雙語合併**：`README.en.md` → `README.md`、`apply-fixes-scope.en.md` → `apply-fixes-scope.md`、`real-scanner-ci-lane.en.md` → `real-scanner-ci-lane.md`（AA3 處理）
  > **移除候選**：`docs/archive/release/` 全部 11 個文件已被現行文件取代（release-checklist→release-playbook、baseline-debt-lifecycle→baseline-governance、first-run-smoke/demo-package-walkthrough→adoption-smoke-guide、risk-calibration/replay-verification-lane→advanced-ci-gates 等）。**Batch 文檔-1**
- [ ] **AA2** 收斂正式文件集合，只保留一組人類能獨立閱讀的最小文件集。預設保留目標：`README.md`、`docs/README.md`、`docs/release/release-playbook.md`、`docs/release/apply-fixes-scope.md`、`docs/release/baseline-governance.md`、`docs/release/supported-environments.md`、`docs/testing/testing-playbook.md`、`CONTRIBUTING.md`、`SECURITY.md`。執行方式：1) 將重複內容併入保留文件；2) 對被取代文件加上簡短導向或直接刪除；3) 更新索引連結避免斷鏈。完成標準：人類只看保留文件就能理解專案用途、安裝/操作方式、限制、測試方式與維運流程。
  > 📋 **決策：** 文件收斂後的人類可讀集合必須可獨立理解專案用途、操作方式、限制與維運流程。**Batch 文檔-2**
  > 執行順序：先 `docs/release/`，再 `docs/testing/`，最後處理 repo 根目錄文件。
- [ ] **AA3** 在保留文件名單確定後，再逐檔做雙語整理。執行方式：1) 中文作為主檔 `.md`；2) 英文保留為對應 `.en.md`；3) 一次只處理一個檔案；4) 優先手工重寫，不使用整批機器翻譯覆寫；5) 每完成一份就檢查標題、表格、連結、code block 是否正常。完成標準：每份保留文件都有可閱讀的繁中主檔與英文副本，且格式未損壞。
  > 📋 **決策：** 翻譯僅套用到最後保留的正式文件；artifact/sample output 不翻。**Batch 文檔-3**
  > 排除範圍：`docs/automations/`、`skills/`、workspace skill 複本、測試快照、sample output、報告樣本。
- [x] **AA4** `docs/automations/` 保持為 AI agent 控制層，不做對外文件翻譯，但這裡的決策必須回流到正式文件。執行方式：1) 當本區塊做出保留/刪除/合併決策時，同步更新對應正式文件；2) 不在 `docs/automations/` 保存只有 agent 看得懂但正式文件沒有的核心知識；3) 若正式流程已改變，優先更新 `README.md` 或 `docs/release/release-playbook.md`。完成標準：正式文件與 agent 決策不脫節。
  > 已驗證 5 項 agent 決策在正式文件中的反映情況，並補上兩個缺口：1) `README.md`/`README.en.md` 加入 MCP 唯讀限制與 `requirements.lock` 說明；2) `supported-environments.md` 加入 MCP server 行作為環境矩陣項目。其餘決策（SARIF 2.1.0、Vue SFC、adapter risk-calibration）已正確記錄於對應正式文件。**Batch 文檔-範圍**

---

## 執行批次摘要

| Batch | 項目 | 類型 |
| --- | --- | --- |
| **1** | W1, R4, Q1, Q2, E6, H3, O2, O3, S2, T2 | 低成本快速修正 |
| **2** | A3, B3, S1+I1, I2, F1 | 文件與合約 |
| **3** | R1, R3, R5, Q3 | 程式碼重構 |
| **4** | O1, J2, L1, L2, H1, P1 | 功能實作 |
| **5** | C5, V1+V2, X1, X2, Z3, T3, F2 | 測試補齊 |
| **6** | A5, A6, C1, S4 | MCP 延後 |
| **文檔-1** | AA1 | 先定義權威文件名單與收斂原則 |
| **文檔-2** | AA2 | 合併/刪減並保留人類可讀文件集合 |
| **文檔-3** | AA3 | 對保留文件做雙語化整理 |
| **文檔-範圍** | AA4 | agent 控制文件與正式文件的邊界 |
| **關閉** | C2, D1, D2, D4, T1, F3 | 暫不處理/維持現狀 |

*此清單由自動化掃描產生。最後一次增量掃描：2026-03-28 #12。*
*決策標註日期：2026-03-28。詳細執行計畫見 [roadmap-execution-plan.md](roadmap-execution-plan.md)。*
