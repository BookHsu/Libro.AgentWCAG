# Libro.AgentWCAG 跨代理 WCAG Skill Repository — 待辦事項

> 掃描時間：2026-03-28（增量更新 #2）
> 目標：完善跨代理的 WCAG 網頁無障礙 skill repository，讓 Codex、Claude、Gemini、Copilot 以一致方式執行無障礙稽核、提出修正建議，並在明確授權下套用安全範圍內的自動修正。

---

## A. 跨代理一致性（Vendor-Neutral Contract）

- [x] **A1** Copilot 缺少 workspace skill 探索目錄：`.copilot/skills/libro-wcag/SKILL.md` 不存在，Claude（`.claude/`）和 Gemini（`.gemini/`）皆已有對應複本。
- [x] **A2** Codex 缺少 workspace skill 探索目錄：`.codex/skills/libro-wcag/SKILL.md` 不存在，僅有空殼 `environment.toml`。
- [ ] **A3** 各 adapter 的 `prompt-template.md` 缺少進階 CLI 選項指引（如 `--policy-bundle`、`--fail-on`、`--baseline`、`--debt-trend-window`），跨代理使用進階功能時需手動查閱 CLI 文件。
- [x] **A4** `skills/libro-wcag/agents/` 僅有 `openai.yaml`，缺少 Claude、Gemini、Copilot 對應的 agent manifest 定義檔。
- [ ] **A5** MCP server 的 `libro_wcag_audit` 工具僅暴露基本參數（target、mode、version），未暴露進階功能（`--policy-bundle`、`--fail-on`、`--baseline`、`--summary-only`、`--report-format sarif`），限制了 MCP 用戶端的能力。
- [ ] **A6** MCP server 缺少 `apply-fixes` 模式的 MCP tool 支援——`libro_wcag_audit` 預設 `audit-only`，且未將修正結果（diff artifacts）回傳給呼叫者。

## B. Schema 與合約強化

- [ ] **B1** JSON Schema (`wcag-report-1.0.0.schema.json`) 對多數欄位使用 `additionalProperties: true`，缺少對 `findings[].fixability`、`findings[].verification_status`、`findings[].manual_review_required`、`fixes[].fixability` 等 core-spec 定義欄位的顯式驗證。
- [ ] **B2** Schema 未定義 `findings[].confidence`、`findings[].sc`、`fixes[].remediation_priority`、`fixes[].framework_hints` 等文件中規定的欄位，使得外部消費者難以做嚴格驗證。
- [ ] **B3** Schema 無版本遷移策略——當 schema 從 `1.0.0` 升級時，缺少 `$schema` 版本比較或向後相容驗證合約。
- [ ] **B4** `summary` 物件在 schema 中僅要求 `total_findings`，未驗證 `remediation_lifecycle`、`diff_summary`、`debt_trend` 等已在 core-spec 中記載的欄位。

## C. 測試覆蓋率強化

- [ ] **C1** MCP server (`mcp-server/`) 缺少自動化整合測試——`test_mcp_server.py` 存在但未涵蓋 `apply-fixes` 模式回傳、錯誤處理、及 SARIF 格式輸出。
- [ ] **C2** 缺少跨平台（Windows / macOS / Linux）CI 矩陣——`test.yml` 僅在 `ubuntu-latest` 上執行，但 `scanner_runtime.py` 有 Windows 特定的 `npx.cmd` 邏輯未被 CI 驗證。
- [ ] **C3** 測試中有 6 個 skipped tests，未記錄跳過原因及預期恢復時間。
- [ ] **C4** 缺少 `pyproject.toml` ↔ `package.json` ↔ `plugin.json` ↔ `marketplace.json` 四處版本號一致性的自動化測試。
- [ ] **C5** 缺少 MCP tool schema 與 SKILL.md 宣告一致性的合約測試（如：MCP 所暴露的參數預設值需與 SKILL.md 一致）。

## D. 發佈與 CI/CD 完善

- [ ] **D1** `publish-npm.yml` 缺少 `NODE_AUTH_TOKEN` 的明確文件或 secret 設定說明，新維護者可能無法順利發佈。
- [ ] **D2** `release.yml` 和 `publish-npm.yml` 分別管理測試與驗證步驟，存在重複邏輯——可提取為 reusable workflow。
- [x] **D3** ~~`plugin.json` / `marketplace.json` 版本同步~~ — 已確認 `scripts/apply-release-version.py` 的 `update_claude_plugin()` 會同步更新 `plugin.json` 和 `marketplace.json`。（誤報，已修正）
- [ ] **D4** npm 發佈流程中 `npm publish` 未明確帶 `--provenance` flag——雖然可由 registry 政策觸發，但顯式宣告更安全且可審計。
- [x] **D5** ~~`dist/` 目錄追蹤問題~~ — 已確認 `dist/` 未被 git 追蹤，僅為本地建置產物。（誤報，已修正）

## E. 文件與開發者體驗

- [ ] **E1** 缺少 `CONTRIBUTING.md`——跨代理 skill repository 需要明確說明貢獻流程、adapter 新增規範、合約變更審查要求。
- [ ] **E2** 缺少 `SECURITY.md`——開源專案應揭示漏洞回報管道，特別是 auto-fix 引擎涉及檔案修改行為。
- [ ] **E3** `AGENTS.md` 未被 git 追蹤（目前為 untracked）——應決定是否納入版控。
- [ ] **E4** `docs/README.md` 僅為索引頁，缺少對 `docs/` 下各子目錄（archive、release、testing、policy-bundles、examples、automations）的導覽說明。
- [ ] **E5** adapter 的 `usage-example.md` 中缺少 MCP 配置範例（如何在 Claude / Copilot / Gemini 中透過 MCP 使用 libro-wcag）的交叉引用。
- [ ] **E6** `README.md` 和 `README.en.md` 中 Claude Marketplace 安裝指令 (`/plugin marketplace add`) 為自定義語法，需確認是否為已實作的 Claude Code 功能，或僅為預期功能的佔位。

## F. 自動修正引擎擴充

- [ ] **F1** `auto_fix.py` 目前支援的規則集中於 HTML 靜態重寫——缺少 color contrast（`color-contrast`）的 CSS 修正支援（即使為 suggest-only 降級，也應明確在修正策略庫中標記為不可自動修正並說明原因）。
- [ ] **F2** `auto_fix.py` 的 regex-based 重寫策略未處理含 `<template>` 或 `<script>` 標籤的複雜 Vue SFC 結構——`rewrite_helpers.py` 有框架支援，但整合路徑未完全測試。
- [ ] **F3** `apply-fixes` 模式的 diff artifacts 缺少機器可讀的 per-hunk metadata（affected rule、affected element selector），使得 CI PR annotation 只能做到 file-level 而非 line-level。

## G. Policy 與 Baseline 治理

- [ ] **G1** Policy bundle 檔案（`docs/policy-bundles/*.json`）缺少 JSON Schema 驗證——`validate_skill.py --validate-policy-bundles` 存在但未驗證 bundle 內部欄位是否符合 `ALLOWED_POLICY_CONFIG_KEYS` 定義。
- [ ] **G2** Baseline governance 文件提到 debt waiver 但缺少 waiver 模板範例——新團隊導入時不易理解如何正確設定 `owner`、`approved_at`、`expires_at`、`reason`。
- [ ] **G3** `--risk-calibration-source` 和 `--stability-baseline` 的文件範例僅在 release docs 中，缺少 adapter-level 或 MCP-level 的整合指引。

## H. 安全性與供應鏈

- [ ] **H1** `mcp-server/server.py` 中 `audit_page.py` 透過 `subprocess.run` 執行外部命令——target 參數來自 MCP 呼叫者，需確認是否有 shell injection 防護（目前 `shell=False` 是正確的，但 `target` 字串仍可能含有路徑穿越）。
- [x] **H2** ~~`.pyc` 檔案追蹤問題~~ — 已確認 `.pyc` 未被 git 追蹤，`.gitignore` 規則生效。（誤報，已修正）
- [ ] **H3** `mcp-server/requirements.txt` 僅列出 `mcp>=1.2,<2`，未鎖定 hash——生產部署時應提供 `requirements.lock` 或 hash-pinned 版本。

## I. 國際化與語言支援

- [ ] **I1** `wcag_workflow.py` 的 `MARKDOWN_COLUMNS` 和 `MARKDOWN_LABELS` 僅支援 `en` 和 `zh-TW`——`output_language` 合約接受任意 BCP-47 值，但實際只有兩種語言的 fallback，缺少文件說明此限制。
- [ ] **I2** adapter prompt-template 中的指示文字（如 Claude adapter 的 "Execution requirements"）為英文，但預設 `output_language=zh-TW`——未說明 prompt 語言與 output 語言的分離策略。

## J. 效能與可擴充性

- [x] **J1** ~~Lighthouse headless 問題~~ — 已確認 `scanner_runtime.py:336` 使用 `--headless=new` 啟動 Chrome，CI 環境已處理。（誤報，已修正）
- [ ] **J2** `run_accessibility_audit.py` 目前為串行執行 axe → Lighthouse——兩個掃描器相互獨立，可平行執行以縮短審計時間。
- [ ] **J3** 大型頁面（>500 findings）時 `normalize_report.py` 的去重邏輯為 O(n²)——缺少效能基準測試或分頁處理策略。

## K. 例外處理與健壯性

- [ ] **K1** `scanner_runtime.py` 中 `_try_run_axe()`（L195）與 `_try_run_lighthouse()`（L399）直接呼叫 `json.load()` 但未 catch `JSONDecodeError`——若 axe/lighthouse 輸出毀損會導致無語義的崩潰訊息。
- [ ] **K2** `wcag_workflow.py` 的 `load_json_file()`（L352）同樣缺少 `JSONDecodeError` 處理——讀取損壞的 baseline report 或 policy config 時會直接崩潰。
- [ ] **K3** `scanner_runtime.py` 的 `_run_command()`（L75-94）未處理 `OSError`、`PermissionError`——可執行檔權限不足時錯誤不會被正確分類為暫時性或永久性失敗。
- [ ] **K4** `subprocess.TimeoutExpired` 在 `scanner_runtime.py:85-86` 被 catch 但未使用例外物件取得逾時指令和實際超時值——錯誤訊息過於泛用。
- [ ] **K5** `scanner_runtime.py` 的 `_wait_for_debug_port()` 使用硬編碼 0.5 秒 socket timeout，未與全域 `timeout_seconds` 參數同步——scanner timeout 在各操作中未一致生效。

## L. 安裝流程與版本管理

- [ ] **L1** `scripts/install-agent.py` 中 `path.parent.mkdir()`（L82）、`path.write_text()`（L88）、`write_manifest()`（L158）缺少 `PermissionError` / `OSError` 處理——安裝中途失敗會留下不完整狀態且無 rollback。
- [ ] **L2** `install-agent.py` 的 `install()` 函式使用 `--force` 取代既有安裝但：未檢查版本相容性、未備份舊版、未警告降級操作、無版本遷移邏輯。
- [ ] **L3** `scripts/validate_skill.py` 未驗證各 adapter 是否完整包含必要檔案（`prompt-template.md`、`usage-example.md`、`failure-guide.md`、`e2e-example.md`），也未驗證 adapter 目錄是否存在。
- [ ] **L4** `scripts/validate_skill.py` 未驗證 SKILL.md frontmatter 是否包含所有必要欄位（僅檢查 name 和 description），也未驗證 scripts 目錄存在。

## M. SARIF 輸出完整性

- [ ] **M1** `run_accessibility_audit.py` 的 `_report_to_sarif()` 缺少 SARIF 2.1.0 規範要求的 `helpUri`/`help` 屬性、`ruleIndex`、`invocation` 物件——生成的 SARIF 不完全符合規範，限制工具互通性。
- [ ] **M2** SARIF results 缺少 `contextRegion` 和 `snippet` 屬性——IDE 和分析工具無法顯示問題周圍的程式碼上下文。
- [ ] **M3** SARIF URI 生成使用 `local_target.as_posix()`（L280），在 Windows 上產出 `C:/path` 而非 RFC 3986 規範的 `file:///C:/path`——部分 SARIF 消費者可能無法識別。
- [ ] **M4** `report_format` 字串在 `run_accessibility_audit.py:383-385` 未經驗證——無效格式值到達此處會靜默產出 SARIF 而無警告。

## N. 資源管理與清理

- [x] **N1** ~~file server thread 清理問題~~ — 已確認 `_try_run_lighthouse()` 的 `finally` 區塊（L382-395）會呼叫 `file_server.shutdown()` + `server_close()` + `join(timeout=5)` 並終止 browser process。（誤報，已修正）
- [ ] **N2** `skills/libro-wcag/scripts/` 和 `skills/libro-wcag/scripts/tests/` 缺少 `__init__.py`——在某些 Python 配置下（site-packages 安裝或 namespace 隔離）可能導致 import 錯誤。
- [ ] **N3** 缺少 `py.typed` marker file——儘管核心模組有大量 type annotation，型別檢查工具（mypy/pyright）不會將此套件識別為 typed package。

## O. 文件與實作落差

- [ ] **O1** `adapters/copilot/e2e-example.md` 聲稱 create 模式在目標不存在時會「產出 manual-review guidance 而非假掃描結果」，但 `scanner_runtime.py` 實際會以「Target file does not exist」錯誤中斷，無法優雅降級。
- [ ] **O2** 單一 scanner 失敗時，`wcag_workflow.py` 會在 `notes` 和 `summary.scanner_failures` 中記錄並插入 manual fallback finding，但 Markdown 輸出的摘要行未以醒目方式（如警告標記）提示報告基於不完整資料——使用者可能忽略 notes 區段中的 scanner 失敗訊息。
- [ ] **O3** `run_accessibility_audit.py:806-807` 設定 `report_schema_version` 但未驗證實際載入的 schema 檔案是否與預期版本匹配——schema 版本不一致可能被靜默記錄。

## P. Policy Bundle 驗證深化

- [ ] **P1** `validate_skill.py` 的 `_validate_single_policy_bundle()` 未驗證 `include_rules` / `ignore_rules` 中的 rule ID 是否為有效的 axe/lighthouse 規則——可建立含不存在規則的 policy bundle 而無警告。

## Q. Reusable Workflow 與消費者體驗

- [ ] **Q1** `install-skill.yml` 硬編碼 `repository: BookHsu/Libro.AgentWCAG.clean` 而非使用 `${{ github.repository }}`——fork 使用者無法直接呼叫此 reusable workflow。
- [ ] **Q2** `install-skill.yml` 未提供 `doctor` 失敗時的 output（如 `installation_valid: true/false`），呼叫方無法根據驗證結果做條件邏輯。
- [ ] **Q3** `libro-wcag-real-scanner.yml` 的 SARIF 建置步驟使用 inline Python heredoc 直接匯入 `run_accessibility_audit` 內部函式 (`_report_to_sarif`)——此為私有 API，缺少穩定性合約保障。

## R. 程式碼品質與可維護性

- [ ] **R1** `auto_fix.py` 中多個 `_fix_*` 函式（`_fix_button_name`、`_fix_link_name`、`_fix_role_name`）共享幾乎相同的「找到無 accessible name 的元素 → 注入 aria-label」邏輯——可提取共用的 `_fix_missing_accessible_name(tag_name, ...)` 以降低重複。
- [ ] **R2** `rewrite_helpers.py` 的 `typing.Pattern` import 在 Python 3.12+ 已被棄用（應使用 `re.Pattern`）——雖然不影響執行但會產生 deprecation warning。
- [ ] **R3** `wcag_workflow.py` 中 `AXE_RULE_TO_SC` 和 `LIGHTHOUSE_RULE_TO_SC` 存在大量重複的 rule → SC 對應——可提取為單一 `SCANNER_RULE_TO_SC` 合併表，降低維護負擔並確保兩份表的一致性。
- [ ] **R4** `scanner_runtime.py` 中 `_find_browser_executable()` 未考慮 macOS 的 `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` 路徑——macOS 使用者若未建立 symlink 可能無法找到 Chrome。
- [ ] **R5** `auto_fix.py` 的 `_guess_accessible_name()` 使用多個 attribute 作為 fallback 猜測名稱（aria-label → title → placeholder → name → id），但在產出的 diff description 中僅顯示最終值，未記錄猜測來源——對審查者不透明。

## S. 合約完整性與邊界行為

- [ ] **S1** `SKILL.md` 和 `core-spec.md` 定義 `output_language` 預設為 `zh-TW`，但合約聲稱接受任意 BCP-47 值。`wcag_workflow.py:resolve_contract()` 未驗證 `output_language` 是否為有效的 BCP-47 tag——無效值會被靜默接受並在輸出中原樣使用。
- [ ] **S2** `normalize_report.py` CLI 的 `--output-json` / `--output-md` 路徑未檢查父目錄是否存在——若提供不存在的目錄路徑會丟出原始 `FileNotFoundError` 而無友善錯誤訊息。
- [ ] **S3** `wcag_workflow.py` 的 `build_citation_url()` 對 WCAG 2.0 產出的 Understanding URL 路徑與 2.1/2.2 相同（使用 `WCAG22/Understanding/`），但 W3C 2.0 的 Understanding 文件實際位於不同路徑（`WCAG20/Understanding/`）——產出的引用 URL 對 WCAG 2.0 模式可能無效。
- [ ] **S4** `mcp-server/tools/suggest_fixes.py` 將 `execution_mode` 硬編碼為 `suggest-only`（L30），即使 MCP 呼叫者預期使用其他模式——MCP suggest tool 無法與 `apply-fixes` 整合。

## T. CI 流程健全性

- [ ] **T1** `test.yml` 的 `dependency-audit` job 受 `vars.LIBRO_RUN_DEPENDENCY_AUDIT == '1'` 條件控制，但未在 release workflow 中強制執行——可能在含已知漏洞的相依套件下發佈。
- [ ] **T2** `publish-npm.yml` 在 `npm publish` 前未執行 `npm pack --dry-run` 驗證打包內容——若 `.npmignore` 或 `files` 設定有誤，可能發佈不完整套件。
- [ ] **T3** `release.yml` 的 `clean-release-smoke` 僅測試 `codex` agent 的安裝冒煙——未涵蓋 `claude`、`gemini`、`copilot`，無法確保所有 agent zip 套件均可正確解壓安裝。

---

*此清單由自動化掃描產生，每小時更新一次。最後一次增量掃描：2026-03-28 #2。*
*已修正誤報：D3、D5、H2、J1、N1（標記為 [x]）。*
