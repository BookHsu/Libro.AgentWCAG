name: Install Libro.AgentWCAG Consumer Sample

on:
  workflow_dispatch:

jobs:
  setup-libro-wcag:
    uses: BookHsu/Libro.AgentWCAG/.github/workflows/install-skill.yml@v1
    with:
      agent: claude
      version: master
      force: true
